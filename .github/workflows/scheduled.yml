name: Scheduled workflow
on:
  push:
  schedule:
    - cron: '*/10 * * * *'
    - cron: '*/15 * * * *'
jobs:
  date:
    runs-on: ubuntu-latest
    steps:
      - name: Set time
        run: echo "::set-output name=TIME::$(date)"
        id: set-time
      - name: Running this step at ${{ steps.set-time.outputs.TIME }}
        run: echo "The current time is ${{ steps.set-time.outputs.TIME }}"    
